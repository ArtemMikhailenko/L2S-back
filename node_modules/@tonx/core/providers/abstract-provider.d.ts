export type RunAction = {
    method: 'getMasterchainInfo';
} | {
    method: string;
    params: Record<string, any>;
};
export declare class AbstractProvider {
    #private;
    constructor();
    _perform<T = any>(action: RunAction): Promise<T>;
    get provider(): this;
    getMasterChainInfo(): Promise<any>;
}
