/// <reference types="node" />
import { AbstractSigner } from "./abstract-signer";
import { KeyPair } from "@ton/crypto";
import { TypedDataDomain, TypedDataField } from "~core/types/typed-data";
export declare class BaseSigner extends AbstractSigner {
    #private;
    constructor({ keyPair }: {
        keyPair: KeyPair;
    });
    signMessage(message: string | Buffer): Buffer;
    signTypedData(domain: TypedDataDomain, types: Record<string, TypedDataField[]>, value: Record<string, any>): Promise<Buffer>;
}
